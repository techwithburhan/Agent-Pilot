apiVersion: v1
kind: Service
metadata:
  name: ollama-frontend
  namespace: ollama-agent
spec:
  type: ClusterIP   # kind: use ClusterIP + port-forward (LoadBalancer has no effect locally)
  selector:
    app: frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
