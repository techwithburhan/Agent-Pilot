apiVersion: v1
kind: Service
metadata:
  name: ollama-mongodb
  namespace: ollama-agent
spec:
  clusterIP: None   # Headless — required for StatefulSet DNS
  selector:
    app: mongodb
  ports:
    - name: mongo
      port: 27017
      targetPort: 27017
