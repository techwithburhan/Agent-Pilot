apiVersion: v1
kind: Service
metadata:
  name: ollama-frontend-service
  namespace: ollama-agent
  labels:
    app: ollama-frontend
spec:
  type: ClusterIP
  selector:
    app: ollama-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
